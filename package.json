{
  "homepage": "http://G-vans/github.io/Bank-of-Flatiron-Phase-2-Code-Challenge",
  "name": "react-hooks-bank-of-flatiron-code-challenge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "^2.1.3",
    "semantic-ui-css": "git+https://github.com/Semantic-Org/Semantic-UI-CSS.git"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "json-server": "^0.17.0"
  },
  "scripts": {
    "start": "PORT=8000 react-scripts start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "server": "json-server --watch db.json -p 8001"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
